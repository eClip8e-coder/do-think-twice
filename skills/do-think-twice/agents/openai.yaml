interface:
  display_name: "Do Think Twice"
  short_description: "Check value, risk, and certainty before suggesting changes."
  default_prompt: "Use do-think-twice to compare keeping the original against each proposed change before recommending edits."
